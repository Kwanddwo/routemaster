from django.db import models
from django.contrib.auth.models import AbstractUser

class User(AbstractUser):
    pass

# Define classes
# Create different user classes for clients, drivers and admins

